# 변경 이력 (릴리스 노트)

버전은 `app/build.gradle.kts`의 `versionName` / `versionCode` 기준입니다.
v1.1부터는 고정 서명 키를 쓰므로 앱 삭제 없이 덮어쓰기 업데이트가 됩니다
(v1.0에서 올라올 때만 1회 삭제 후 재설치 필요).

---

## v1.4 (versionCode 5) — 2026-09-01

- 새로고침 간격 선택지에 **2·3·4초** 추가 (기존: 1, 5, 10, 15, 30, 60초).

## v1.3 (versionCode 4) — 2026-09-01

**세션 만료("로그아웃되었습니다. 다시 로그인하여 주십시오.") 근본 원인 수정**

- 쿠키 저장소가 응답이 올 때마다 쿠키 목록을 **통째로 교체**해서, 검색/예약 응답이
  로그인과 무관한 쿠키(로드밸런서·추적용)를 하나라도 내려보내면 `JSESSIONID`가
  사라졌습니다. 그 결과 예약 단계에서 세션이 끊겨 실패 → 재로그인이 반복됐습니다.
  → 이제 쿠키를 **이름별로 병합**(만료 처리 포함)하여 `JSESSIONID`가 유지됩니다.
- 예약 도중 세션이 끊기면 다음 조회를 기다리지 않고 **같은 열차로 즉시 재시도**합니다
  (좌석이 사라지기 전에).

> 참고: 세션 만료를 감지해 재로그인하는 로직 자체는 v1.0(2026-08-21)에 이미 있었지만,
> 그건 증상 대응이었고 v1.3이 원인을 없앤 것입니다. **v1.2에는 이 수정이 없습니다.**

## v1.2 (versionCode 3) — 2026-09-01

- 좌석 등급에 **"일반+특실"** 옵션 추가. 선택한 등급 중 먼저 빈자리가 뜨는 쪽을 예약하고,
  한 열차에 일반·특실이 모두 있으면 **일반실 우선**으로 잡습니다.

## v1.1 (versionCode 2) — 2026-09-01

- **날짜·시각 피커에 "분" 선택 추가** (5분 단위). 기존에는 시 단위(HH00)만 가능했습니다.
- **지난 날짜/시각을 고르면 이유를 명확히 표시**하고 시작을 막습니다
  (예: "지난 날짜는 예매할 수 없습니다", "오늘 이미 지난 시각입니다").
- 새로고침 간격에 **1초** 옵션 추가 (계정 제한 주의 문구 포함).
- **실행 로그** 개선: 영역 확대, 스크롤바 상시 표시, 텍스트 선택 가능, 페이지와
  독립적으로 스크롤, **"크게 보기"** 전체화면 로그.
- 강릉선 KTX 역 추가: **서원주·만종·횡성·둔내·평창·진부** (기존 역 목록이 2017년
  강릉선 개통 이전 자료라 빠져 있었음). 역 목록 가나다 정렬.
- **고정 서명 키 도입**(`signing/korail-macro.jks`) — debug/release 모두 같은 키로
  서명되어, 이후로는 삭제 없이 덮어쓰기 업데이트 시 로그인 정보·텔레그램 설정이 유지됩니다.
- 화면 상단 제목 옆에 앱 버전 표시.

## v1.0 (versionCode 1) — 2026-08-14 ~ 08-23

첫 사용 가능 버전. 주요 내용:

- 코레일 로그인 + 열차 조회 + 빈 좌석(취소표) 발견 시 자동 **예약**(결제는 수동) +
  텔레그램 알림. 포그라운드 서비스로 화면이 꺼져도 동작.
- 로그인/조회 크래시 버그 수정, 역·날짜·열차 종류 선택 UI 추가.
- 다운로드용 debug APK, 릴리스 서명 설정 + 서명된 release APK.
- 다크/라이트 모드 토글.
- 로그인·간격·테마·텔레그램 설정을 **설정 다이얼로그**로 정리.
- 오른쪽으로 스와이프하면 **좌석 현황 화면** (매크로와 독립된 1회성 조회).
- 다크모드에서 안 보이던 달력, 필터 안 되던 좌석현황 목록 수정.
- R8/리소스 축소 활성화, 코레일·텔레그램 **TLS 인증서 피닝**.
- 실행 로그 영역 고정 높이 + 자체 스크롤.
- 검색 페이지네이션 상한 수정 + **세션 만료 시 재로그인** 로직 추가.
- 프로세스가 죽은 뒤 서비스가 **혼자 재시작하지 않도록** 변경 (의도치 않은 예매 방지).
- 요청한 종료시각을 지나면 검색 페이지네이션 중단.
- 사용자가 실제로 검색한 조건(출발·도착·시간대·열차종류)에 맞는 열차만 표시/집계.
